services:
  discord-bot:
    build: .
    container_name: liama6-discord-bot
    restart: unless-stopped
    ports:
      - "3000:3000"  # API server
    environment:
      # Discord Bot Token (required)
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      
      # Alpha Vantage API Key (optional, for stock features)
      - ALPHA_VANTAGE_API_KEY=${ALPHA_VANTAGE_API_KEY:-demo}
      
      # API Server Port
      - API_PORT=3000
      
      # Node Environment
      - NODE_ENV=production
      
      # Twitch API Credentials (optional, for Twitch features)
      - TWITCH_CLIENT_ID=${TWITCH_CLIENT_ID}
      - TWITCH_CLIENT_SECRET=${TWITCH_CLIENT_SECRET}
    volumes:
      # Persist logs and config
      - ./data:/app/data
    networks:
      - bot-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/config', (res) => { process.exit(res.statusCode === 200 ? 0 : 1) })"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Dashboard (Vue frontend)
  dashboard:
    build:
      context: ./dashboard
      dockerfile: Dockerfile
    container_name: liama6-dashboard
    restart: unless-stopped
    ports:
      - "5173:5173"  # Vite dev server
    environment:
      - VITE_API_URL=http://discord-bot:3000
    volumes:
      - ./dashboard:/app
      - /app/node_modules
    depends_on:
      discord-bot:
        condition: service_healthy
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge

volumes:
  data:
